# Minimal dependency set for running the unit test suite.
# Keep versions aligned with requirements.txt.

# Web framework
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.6

# Validation / config
pydantic==2.10.0
pydantic-settings==2.1.0

# LLM client
openai==1.10.0

# Retrieval / vector search
rank-bm25==0.2.2
faiss-cpu==1.8.0
numpy==1.26.4

# Caching / rate limiting / metrics
redis==5.0.1
slowapi>=0.1.9
prometheus-client==0.19.0
prometheus-fastapi-instrumentator==6.0.0
tenacity==8.2.3
httpx==0.27.0

# Batch processing
celery>=5.3.0

# Test tooling
pytest==7.4.3
pytest-asyncio==0.23.2
pytest-cov==4.1.0
pytest-mock==3.15.1
